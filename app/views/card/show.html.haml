.wraper
  .sell-btn
    .sell-btn__text 出品
    %i.fas.fa-camera
= render "homes/header"

- breadcrumb :mypage, class: "breadcrumbs"
= breadcrumbs pretext: "",separator: " &rsaquo; "


.mypage
  = render "homes/side-bar"
  .credit__main2
    .credit__main2__top
      登録クレジットカード情報
    .credit__main3__top
      クレジットカード一覧
    .credit__main3__number
      = "**** **** **** " + @default_card_information.last4
      .credit__main3__delete
        = form_tag(delete_card_index_path, method: :post, id: 'charge-form',  name: "inputForm") do
          %input{ type: "hidden", name: "card_id", value: "" }
          %button 削除する
    .credit__main3__date
      - exp_month = @default_card_information.exp_month.to_s
      - exp_year = @default_card_information.exp_year.to_s.slice(2,3)
      = exp_month + " / " + exp_year
    .credit__main3__footer
      支払い方法について  ＞

= render "homes/footer"